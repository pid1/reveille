[project]
name = "reveille"
version = "0.1.0"
description = "highland village daily plaintext briefing"
requires-python = ">=3.14"
dependencies = [
    "feedparser>=6.0",
]

[tool.uv]
# default settings are fine
